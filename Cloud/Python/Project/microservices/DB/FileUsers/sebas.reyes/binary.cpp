//#include <vector.h>

#include <bits/stdc++.h>

using namespace std;
/*
int binary_search(int low, int high, int element, vector<int> A) {

   while(high > low) {
      int mid = (low + high)>>1;
      if (mid <= element)  low = mid;
      else high = mid;
    }
   return low;
}
*/
int main() {
   cout << "Hola mundo Ramirez" << endl;
   //cout << binary_search(0, 4, 4, {1,2,3,4}) << endl;
   return 0;
}

